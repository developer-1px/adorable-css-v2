# Smart Containers

Smart Containers are AdorableCSS's revolutionary approach to creating perfect container elements with minimal code. The `64x64` syntax isn't just width and height - it's a complete layout solution.

## What Makes Them "Smart"?

Traditional CSS requires multiple properties to create a well-behaved container:

```css
.container {
  width: 64px;
  height: 64px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  position: relative;
}

.container img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}
```

With Smart Containers, this becomes:

```html
<div class="64x64">
  <img src="avatar.jpg" />
</div>
```

## Built-in Features

Smart Containers automatically provide:

### 🎯 Auto-Centering
Content is automatically centered using flexbox layout - no need for manual `display(flex) align-items(center) justify-content(center)`.

### 🖼️ Image Optimization
Child images automatically get:
- `object-fit: cover` for perfect cropping
- `object-position: center` for optimal positioning
- `width: 100%; height: 100%` for full container fill

### 🛡️ Content Protection
`overflow: hidden` prevents content from breaking your layout.

### 📍 Positioning Ready
`position: relative` allows absolute positioning of child elements.

## Use Cases

Smart Containers excel in common UI patterns:

### Profile Images & Avatars

```html
<div class="64x64 r(full)">
  <img src="user-avatar.jpg" alt="User" />
</div>
```

### Icon Containers

```html
<button class="48x48 bg(primary) r(lg) hover:scale(1.05) transition">
  ⚙️
</button>
```

### Thumbnails

```html
<div class="120x80 r(md) shadow(md)">
  <img src="thumbnail.jpg" alt="Preview" />
</div>
```

### Badge Containers

```html
<div class="32x32 bg(red-500) r(full)">
  <span class="font(sm) c(white) font(600)">5</span>
</div>
```

## Syntax Variations

Smart Containers support any valid CSS unit:

```html
<!-- Pixels (default) -->
<div class="64x64">...</div>

<!-- Percentages -->
<div class="100%x50%">...</div>

<!-- Mixed units -->
<div class="200pxx4rem">...</div>

<!-- Viewport units -->
<div class="50vwx100vh">...</div>
```

## vs. Basic Dimensions

Understanding when to use Smart Containers vs. basic dimensions:

### Use Smart Containers (`64x64`) when:
- Creating image containers
- Building icon buttons
- Making profile pictures
- Designing thumbnails
- Any container that needs centering

### Use Basic Dimensions (`w(64) h(64)`) when:
- Building form inputs
- Creating pure layout containers
- You need specific positioning behavior
- Working with non-centered content

## Advanced Examples

### Notification Badge

```html
<div class="relative">
  <div class="80x80 bg(blue-100) r(xl)">
    <Mail size="32" class="c(blue-600)" />
  </div>
  <div class="layer(top:-8px+right:-8px) 24x24 bg(red-500) r(full) shadow(lg)">
    <span class="font(xs) c(white) font(600)">3</span>
  </div>
</div>
```

### Interactive Profile

```html
<button class="group">
  <div class="64x64 r(full) group-hover:scale(1.1) transition">
    <img src="profile.jpg" alt="Profile" />
  </div>
</button>
```

### Logo Container

```html
<div class="40x40 bg(135deg/#8b5cf6..#ec4899) r(12px) shadow(md)">
  <svg class="c(white)" width="20" height="20">
    <!-- Logo SVG -->
  </svg>
</div>
```

## Performance Benefits

Smart Containers provide:

- **Reduced CSS bundle size**: One utility instead of 5-7 classes
- **Better caching**: Consistent patterns across your app
- **Fewer reflows**: Optimized for modern browser rendering
- **Predictable behavior**: Same centering logic everywhere

## Migration Guide

Converting existing containers to Smart Containers:

```html
<!-- Before -->
<div class="w(64) h(64) display(flex) align-items(center) justify-content(center) overflow(hidden)">
  <img src="avatar.jpg" style="object-fit: cover;" />
</div>

<!-- After -->
<div class="64x64">
  <img src="avatar.jpg" />
</div>
```

The Smart Container approach saves you **75% of the CSS classes** while providing **more functionality** out of the box.

Ready to make your containers smarter? Start with your most common image containers and work your way through your component library! ✨